{
  "name": "delay_logstash",
  "version": "1.0.0",
  "description": "logstash延时批量发送",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  
  "dependencies": {
    "logstash-client": "^1.1.1",
    "node-slogger": "^1.0.2"
  }
}
